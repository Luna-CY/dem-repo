name: mongosh
versions:
  - version: 1.6.2
    alias:
      - 1.6
    paths:
      - "{ROOT}/mongosh-{VERSION}-darwin-arm64/bin"
    archive:
      enable: true
      package: "https://downloads.mongodb.com/compass/mongosh-{VERSION}-darwin-arm64.zip"
